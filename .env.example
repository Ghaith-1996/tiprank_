# MongoDB connection string (MongoDB Atlas, etc.)
MONGODB_URI="mongodb+srv://user:password@cluster.mongodb.net/analyst-tracker?retryWrites=true&w=majority"

# Secret key to protect the /api/seed endpoint (set any random string)
SEED_SECRET="change-me-to-a-random-string"

# Gemini API key (optional, for AI features)
GEMINI_API_KEY="MY_GEMINI_API_KEY"
